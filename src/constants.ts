

export const PORT_SOCKET_SERVER = 9009;


// TODO: These are web-app specific, and SvgPaper-specific at that. 
// Should be moved and incorporated into a config for the view.
export const DEFAULT_CARRIAGE = 10;
export const CELL_WIDTH = 10;
export const CELL_HEIGHT = 20;
export const NUM_ROWS = 35;
export const NUM_COLS = 60;